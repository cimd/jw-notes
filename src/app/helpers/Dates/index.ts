import { dateToDisplay } from './DateToDisplay'
import { dateToField } from './DateToField'
import { dateToInput } from './DateToInput'

const date = {
  toDisplay: dateToDisplay,
  toField: dateToField,
  toInput: dateToInput,
}

export default date
